pip install numpy --> any version can come
pip install numpy==2.4.2 --> specific version (2.4.2) version will come
pip install numpy>=2.4.2 --> (2.4.2) version or any version greater than that will come
pip install numpy<=2.4.2 --> (2.4.2) version or any version smaller than that will come